<UserControl x:Class="MyLoadTest.SapIDocGenerator.UI.Controls.PathSelectionControl"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" mc:Ignorable="d" d:DesignWidth="300"
        MinWidth="150" Height="Auto">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <TextBox Grid.Column="0" Name="FilePathTextBox" Margin="0,5,0,5" IsReadOnly="True"
                TextChanged="FilePathTextBox_TextChanged" />
        <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Stretch"
                VerticalAlignment="Stretch">
            <Button Name="BrowseButton" Content="Browse..." MinWidth="80" Margin="5,0,0,0"
                    Click="BrowseButton_Click" />
            <Button Name="ClearButton" Content="Clear" MinWidth="80" Margin="5,0,0,0" Click="ClearButton_Click" />
        </StackPanel>
    </Grid>
</UserControl>